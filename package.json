{
  "name": "triage-desk",
  "version": "0.1.0",
  "private": true,
  "description": "Internal Trust & Safety / CX triage tool: policy-grounded classification with guardrails, evals, and LLM-Ops telemetry.",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "test": "bun test",
    "eval": "bun evals/run.ts",
    "mcp": "bun mcp/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@modelcontextprotocol/sdk": "^1.22.0",
    "hono": "^4.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
